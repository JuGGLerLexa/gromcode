const countArea = (chickens, caws, pigs) => chickens * 0.2 + caws * 1.5 + pigs * 0.5;

console.log(countArea(10, 10, 10)); // ===> 22
console.log(countArea(10, 0, 1)); // ===> 2.5
console.log(countArea(6, 3, 5)); // ===> 8.2
